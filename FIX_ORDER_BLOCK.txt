const order = new Order({
  business,
  total,

  status: "UNPAID",

  customerPhone: req.body.customerPhone || "254700000001",
  customerUserId: req.body.customerUserId || "SYSTEM",

  businessWalletId: business,

  items: items.map(item => ({
    product: item.productId,
    qty: item.qty
  })),

  createdAt: new Date()
});
