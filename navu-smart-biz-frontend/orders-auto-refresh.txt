/* ================= ORDER AUTO REFRESH ================= */

setInterval(() => {

  const ordersView =
    document.getElementById(
      "orders"
    );

  if (
    ordersView &&
    !ordersView.classList.contains(
      "hidden"
    )
  ) {

    loadOrders();
  }

}, 10000);
